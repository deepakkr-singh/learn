# AWS Configuration
AWS_CLIENT_ID=your_aws_client_id
AWS_CLIENT_SECRET=your_aws_client_secret
AWS_REGION=us-east-1
AWS_SECRETS_MANAGER_NAME=your-secret-name

# Azure Configuration
AZURE_CLIENT_ID=your_azure_client_id
AZURE_CLIENT_SECRET=your_azure_client_secret
AZURE_TENANT_ID=your_azure_tenant_id
AZURE_KEY_VAULT_URL=https://your-keyvault.vault.azure.net/

# Cloud Provider Selection (aws, azure, env)
CLOUD_PROVIDER=env

# ====================================================================
# Cloud-Based AI PII Detection (Optional)
# ====================================================================
# Use cloud AI services for PII detection instead of local regex
# Azure Text Analytics or AWS Comprehend will detect PII in the cloud
# Redaction still happens locally

# Azure Text Analytics (for AI-powered PII detection)
# Setup:
# 1. Create App Registration in Azure AD (get client_id, client_secret, tenant_id)
# 2. Grant "Cognitive Services User" role to App Registration
# 3. Create Azure Cognitive Services → Language Service resource
# 4. Get endpoint from Azure Portal
# IMPORTANT: Uses Azure AD authentication (client_id, client_secret, tenant_id above)
#            NOT API keys for better security
AZURE_TEXT_ANALYTICS_ENDPOINT=https://your-resource.cognitiveservices.azure.com/

# AWS Comprehend (for AI-powered PII detection)
# Uses AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY from above
# Or uses IAM role if running on EC2/Lambda/ECS
AWS_COMPREHEND_REGION=us-east-1
